apiVersion: v1
kind: Service
metadata:
  name: corporate-website-service
spec:
  type: NodePort
  selector:
    app: corporate-website
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
